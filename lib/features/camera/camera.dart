import 'package:flutter/material.dart';

class CamerPage extends StatelessWidget {
  const CamerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("CAmeeeeraaaaaaaaaaaa"),);
  }
}